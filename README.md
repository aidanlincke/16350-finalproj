# 16-350 Final Project

### Compile the Code
1. Run `generate_map.py`
2. (Optional) Install Emscripten
3. (Optional) Run `make`

### Run the Code
1. Run `python -m http.server`
2. Open `localhost:8000` on a browser
